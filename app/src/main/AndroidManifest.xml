<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.baina">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityResult">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".TidSelectActivity" />
        <activity android:name=".StationActivity" />
        <activity android:name=".StationAndStationSelectActivity" />
        <activity android:name=".MethodActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AddStationActivity" />
        <activity android:name=".AddRelativeActivity" />
        <activity android:name=".AddTrainActivity" />
        <activity android:name=".ActivitySetting"></activity>
    </application>

</manifest>